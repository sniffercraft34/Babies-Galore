# Grabs the mobs from the babies.json list and calls a function as them
# This is simpler and easier than running the individual lines of functions as them
execute as @e[type=#babies_galore:babies] at @s run function babies_galore:main/do_random